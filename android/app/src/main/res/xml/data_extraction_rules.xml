<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ replacement of fullBackupContent. Same intent: exclude
  everything from cloud backup and device-to-device transfer.
-->
<data-extraction-rules>
    <!--
      path="." (not omitted) on every rule — see the matching comment in
      backup_rules.xml. Inert today (allowBackup="false"), defense in depth
      only.
    -->
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
